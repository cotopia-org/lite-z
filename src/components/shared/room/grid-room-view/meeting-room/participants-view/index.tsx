import { MeetingNodeType } from "../.."

type Props = {
  userNodes: MeetingNodeType[]
}

const ParticipantsView = ({ userNodes }: Props) => {
  return <div>ParticipantsView</div>
}

export default ParticipantsView
